# clientside-containers configuration
# Copy to .env.local and adjust as needed. The app runs entirely client-side;
# there is no server config.

# Base path when serving under a sub-path (e.g. "/clientside-containers" on
# GitHub Pages). Leave empty when served from a domain root.
PAGES_BASE_PATH=
